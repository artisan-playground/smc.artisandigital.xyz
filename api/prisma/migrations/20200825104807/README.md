# Migration `20200825104807`

This migration has been generated by Parames Chotana at 8/25/2020, 5:48:07 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "Role" AS ENUM ('EMPLOYEE', 'ADMIN')

CREATE TABLE "public"."User" (
"id" SERIAL,
"email" text   NOT NULL ,
"firstName" text   ,
"lastName" text   ,
"phone" text   ,
"role" "Role"  NOT NULL DEFAULT E'EMPLOYEE',
"companyId" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Company" (
"id" integer   NOT NULL ,
"companyName" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Inventory" (
"id" SERIAL,
"productName" text   NOT NULL ,
"quantity" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "User.email_unique" ON "public"."User"("email")

ALTER TABLE "public"."User" ADD FOREIGN KEY ("companyId")REFERENCES "public"."Company"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200825104807
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,36 @@
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator prisma_client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id        Int     @id @default(autoincrement())
+  email     String  @unique
+  firstName String?
+  lastName  String?
+  phone     String?
+  role      Role    @default(EMPLOYEE)
+  company   Company @relation(fields: [companyId], references: [id])
+  companyId Int
+}
+
+model Company {
+  id          Int    @id
+  companyName String
+  users       User[]
+}
+
+model Inventory {
+  id          Int    @id @default(autoincrement())
+  productName String
+  quantity    Int
+}
+
+enum Role {
+  EMPLOYEE
+  ADMIN
+}
```


